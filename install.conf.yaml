- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.zshrc:
    ~/.zsh:
    ~/.gdbinit:
    ~/.config/ghostty/config:
      path: ghostty
    ~/.kube/kubie.yaml:
      path: kubie.yaml
- shell: []
